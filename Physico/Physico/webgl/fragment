#ifdef GL_ES
precision highp float;
#endif

varying vec4 vColor;
varying vec3 vLightWeighting;

void main(void) {
    vec3 color = vec3(vColor);
    gl_FragColor = vec4(color * vLightWeighting, vColor.a);
}